{
  "name": "@arachne/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "bun run src/server/index.ts",
    "build": "tsc --build",
    "test": "bun test src/",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@arachne/shared": "workspace:*",
    "zod": "^4.3.6"
  }
}
