export interface ProfileInfo {
  projectId: string
  name: string
  description: string
  techStack: string[]
  keyConcepts: string[]
  services: string[]
  recentActivity: string
  rawTokenEstimate: number
}
