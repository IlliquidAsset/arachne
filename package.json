{
  "name": "amanda",
  "version": "0.1.0",
  "private": true,
  "description": "Amanda - AI assistant hybrid monorepo",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "tsc --build",
    "test": "bun test",
    "typecheck": "tsc --build --noEmit",
    "clean": "tsc --build --clean"
  },
  "devDependencies": {
    "bun-types": "1.3.6",
    "typescript": "^5.7.3"
  }
}
